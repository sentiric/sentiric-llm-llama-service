# docker-compose.override.yml
# GELİŞTİRME dosyası. 'docker compose up --build' komutu çalıştırıldığında
# bu dosya otomatik olarak temel dosyayı ezer ve yerel CPU build'ini tetikler.
services:
  llm-llama-service:
    build:
      context: .
      dockerfile: Dockerfile